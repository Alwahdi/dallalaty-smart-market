import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alwahdi.matjeribb',
  appName: 'com.alwahdi.matjeribb',
  webDir: 'dist'
};

export default config;
